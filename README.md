### Terraform workflow

`write -> plan -> apply`

### Teraform init

- No need need run each time unless to create a new workspace or add a new provider.

### Terraform import

- terraform import aws_iam_user.import_user <user_name>
